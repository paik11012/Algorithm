## 신고결과받기.py

```
문제 설명
신입사원 무지는 게시판 불량 이용자를 신고하고 처리 결과를 메일로 발송하는 시스템을 개발하려 합니다. 무지가 개발하려는 시스템은 다음과 같습니다.

각 유저는 한 번에 한 명의 유저를 신고할 수 있습니다.
신고 횟수에 제한은 없습니다. 서로 다른 유저를 계속해서 신고할 수 있습니다.
한 유저를 여러 번 신고할 수도 있지만, 동일한 유저에 대한 신고 횟수는 1회로 처리됩니다.
k번 이상 신고된 유저는 게시판 이용이 정지되며, 해당 유저를 신고한 모든 유저에게 정지 사실을 메일로 발송합니다.
유저가 신고한 모든 내용을 취합하여 마지막에 한꺼번에 게시판 이용 정지를 시키면서 정지 메일을 발송합니다.

이용자의 ID가 담긴 문자열 배열 id_list, 각 이용자가 신고한 이용자의 ID 정보가 담긴 문자열 배열 report, 정지 기준이 되는 신고 횟수 k가 매개변수로 주어질 때, 각 유저별로 처리 결과 메일을 받은 횟수를 배열에 담아 return 하도록 solution 함수를 완성해주세요.
return 하는 배열은 id_list에 담긴 id 순서대로 각 유저가 받은 결과 메일 수를 담으면 됩니다.
```

```
들어오는 정보
[id_list, report, k]
["muzi", "frodo", "apeach", "neo"], ["muzi frodo", "apeach frodo", "frodo neo", "muzi neo", "apeach muzi"], 2
내보내야 할 정보
[2,1,1,0]
```

문제풀이

```python
# 1) k번 이상 신고당해 정지당한 유저 찾기
# 2) 그 유저를 신고한 신고자 찾기
# 3) 신고자에게 보낼 메일 횟수를 리스트로 반환하기
# [id_list, report, k]
# (len(id_list))=4 -> 이걸로 [0,0,0,0]
reported=[0]*len(id_list)  # 여기다가 신고 횟수 쌓을 예정

# 이제 신고자/신고당한자를 분리해야 함, 이 둘은 한 칸으로 띄워져 있음
# 분리 후 k번 이상 신고당한 자를 찾아야 함
# report = ["muzi frodo", "apeach frodo", "frodo neo", "muzi neo", "apeach muzi"]
# 신고당한 사람이 뒤에 나오니까 for문을 돌며 신고당한 이름이 일치하면 숫자 올리기
tem_report = [0]*len(id_list)  # 몇번 신고당했는지 저장용 리스트
# 아...띄어쓰기 뒷부분만 가져오는거 어떻게 하지 .split()
for i in report;
    two = i.split() # 누가 누구를 신고했는지 리스트
    for j in id_list:
        if j == two[1]  # 신고 당한 사람이 id_list에 있는 사람과 같다면
        	+=1  # j 순서인 0에 1 더하기(신고 당한 것 표시)
        	



```