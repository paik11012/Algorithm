# "ZRO", "ONE", "TWO", "THR", "FOR", "FIV", "SIX", "SVN", "EGT", "NIN"
# 문자열 비교를 통해 정렬한 후 출력하기. 그러나 문자열 자체를 비교하는게 쉽지 않다
# 0 1 0 2로 가공해서 정렬. 이에 대응하는 문자열 찍기
a = input(str())
def str_comp("ZRO", str):
    arr[j]= 0

# 그리고 버블정렬, 퀵정렬 이용 그러나 좋지 않다
# 그러나 counting sort이용: element가 나누어져 있어야 함, 정수형
# "ZRO", "ONE", "TWO", "THR", "FOR", "FIV", "SIX", "SVN", "EGT", "NIN"
cnt(f(str))+1= 1
# Z R O = Z R O 일일히 비교하는 방법밖에 없다

cnt = [0] * 10
for num in nums:
    getidx(num)

def getidx(num):
    for i in range(10):
        if num[0] == p[i][0] and num[1] == p[i][1] and num[2]  == p[i][2]:
            return i